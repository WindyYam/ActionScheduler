An example framework that utilise the ActionScheduler as the component for managing task functions and low power mode
Enable the RTC, turn on its interrupt, and then pass the instance to AppFramework_Init
For Stm32g0 only, but it is easy to expand to other models